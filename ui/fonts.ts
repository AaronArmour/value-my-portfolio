import { Merriweather } from 'next/font/google';
import { Quattrocento } from "next/font/google";

export const merriweather = Merriweather({ subsets: ['latin']});
export const quattrocento = Quattrocento({ weight: '400', subsets: ['latin']});
